# Environment/runtime configuration only - business/test data lives under
# testdata/*.json, Any value can be overridden (see ConfigReader).
base.url=https://www.globalsqa.com/angularJs-protractor/BankingProject/#/
browser=chrome
headless=true
implicit.wait.seconds=2
explicit.wait.seconds=10
page.load.timeout.seconds=30